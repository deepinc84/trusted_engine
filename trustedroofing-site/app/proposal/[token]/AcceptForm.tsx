"use client";import{useEffect,useRef,useState}from"react";import type{ProposalDraft}from"@/lib/proposals/domain";const money=(n:number)=>new Intl.NumberFormat("en-CA",{style:"currency",currency:"CAD"}).format(n);
export default function AcceptForm({token,proposal}:{token:string;proposal:ProposalDraft}){const[selected,setSelected]=useState(""),[colour,setColour]=useState(""),[name,setName]=useState(proposal.customer.name),[email,setEmail]=useState(proposal.customer.email),[type,setType]=useState<"typed"|"drawn">("typed"),[signature,setSignature]=useState(""),[authority,setAuthority]=useState(false),[terms,setTerms]=useState(false),[state,setState]=useState(""),canvas=useRef<HTMLCanvasElement>(null),drawing=useRef(false);const option=proposal.options.filter(o=>o.included).find(o=>o.tier===selected),total=option?.price??0,subtotal=total/1.05,gst=total-subtotal;useEffect(()=>{const c=canvas.current;if(!c)return;const point=(e:PointerEvent)=>{const r=c.getBoundingClientRect();return{x:(e.clientX-r.left)*c.width/r.width,y:(e.clientY-r.top)*c.height/r.height}};const down=(e:PointerEvent)=>{drawing.current=true;const p=point(e),x=c.getContext("2d")!;x.beginPath();x.moveTo(p.x,p.y);c.setPointerCapture(e.pointerId)};const move=(e:PointerEvent)=>{if(!drawing.current)return;const p=point(e),x=c.getContext("2d")!;x.lineWidth=3;x.lineCap="round";x.lineTo(p.x,p.y);x.stroke()};const up=()=>{drawing.current=false;setSignature(c.toDataURL("image/png"))};c.addEventListener("pointerdown",down);c.addEventListener("pointermove",move);c.addEventListener("pointerup",up);return()=>{c.removeEventListener("pointerdown",down);c.removeEventListener("pointermove",move);c.removeEventListener("pointerup",up)}},[type]);async function accept(){setState("Submitting acceptance…");const r=await fetch(`/proposal/${token}/accept`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({selectedTier:selected,colour,signerLegalName:name,signerEmail:email,signatureType:type,signatureData:type==="typed"?name:signature,authorityAccepted:authority,termsAccepted:terms})});const b=await r.json();if(r.ok)location.reload();else setState(b.error)}return <section className="customer-contract"><h2>Select your roofing system</h2><div className="proposal-card-grid">{proposal.options.filter(o=>o.included).sort((a,b)=>a.displayOrder-b.displayOrder).map(o=><label className={selected===o.tier?"is-selected":""} key={o.tier}><input type="radio" name="option" value={o.tier} checked={selected===o.tier} onChange={()=>setSelected(o.tier)}/><b>{o.tierLabel}</b><h3>{o.productName}</h3><p>{o.description}</p><strong>{money(o.price)}</strong></label>)}</div><h2>Contract acceptance</h2><p>Selected system: {option?`${option.tierLabel} — ${option.productName}`:"Select an option above"}</p><p>Manufacturer/product: {option?.productName??"—"}</p><label>Shingle colour<input required value={colour} onChange={e=>setColour(e.target.value)} placeholder="Enter selection"/></label><label>Signer legal name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Signer email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><fieldset><legend>Electronic signature</legend><label><input type="radio" checked={type==="typed"} onChange={()=>setType("typed")}/> Type my legal name</label><label><input type="radio" checked={type==="drawn"} onChange={()=>{setType("drawn");setSignature("")}}/> Draw signature</label>{type==="typed"?<div className="typed-signature">{name||"Your legal name"}</div>:<><canvas ref={canvas} width="700" height="180"/><button onClick={()=>{canvas.current?.getContext("2d")?.clearRect(0,0,700,180);setSignature("")}}>Clear</button></>}</fieldset><dl className="proposal-prices"><dt>Subtotal</dt><dd>{money(subtotal)}</dd><dt>GST</dt><dd>{money(gst)}</dd><dt>Contract total</dt><dd>{money(total)}</dd><dt>Proposal valid until</dt><dd>{proposal.expiryDate}</dd></dl><label><input type="checkbox" checked={authority} onChange={e=>setAuthority(e.target.checked)}/> I have authority to approve work at this property.</label><label><input type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)}/> I accept the proposal scope, payment terms, and conditions.</label><button className="button" disabled={!selected||!colour||!name||!email||!(type==="typed"?name:signature)||!authority||!terms} onClick={()=>void accept()}>Accept and electronically sign</button><p>{state}</p></section>}
