import{NextRequest,NextResponse}from"next/server";import{acceptProposal}from"@/lib/proposals/signing";import{checkRateLimit,requestIp}from"@/lib/rate-limit";export async function POST(r:NextRequest,{params}:{params:{token:string}}){const ip=requestIp(r),limit=checkRateLimit(`proposal-accept:${ip}`,8,15*60_000);if(!limit.allowed)return NextResponse.json({error:"Too many attempts. Try again later."},{status:429});try{return NextResponse.json(await acceptProposal(params.token,await r.json(),{ip,userAgent:r.headers.get("user-agent")??"unknown"}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Acceptance failed"},{status:400})}}
