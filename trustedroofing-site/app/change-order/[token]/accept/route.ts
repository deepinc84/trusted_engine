import{NextRequest,NextResponse}from"next/server";import{checkRateLimit,requestIp}from"@/lib/rate-limit";import{decideChangeOrder}from"@/lib/projects/repository";export async function POST(r:NextRequest,{params}:{params:{token:string}}){const ip=requestIp(r);if(!checkRateLimit(`co-decision:${ip}`,8,900000).allowed)return NextResponse.json({error:"Too many attempts. Try again later."},{status:429});try{return NextResponse.json(await decideChangeOrder(params.token,await r.json(),{ip,userAgent:r.headers.get("user-agent")??"unknown"}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Decision failed"},{status:400})}}
