"use client";import{useState}from"react";export default function DecisionForm({token,allowDecline}:{token:string;allowDecline:boolean}){const[name,setName]=useState(""),[title,setTitle]=useState(""),[email,setEmail]=useState(""),[signature,setSignature]=useState(""),[note,setNote]=useState(""),[accepted,setAccepted]=useState(false),[message,setMessage]=useState("");async function decide(decision:"approved"|"declined"){setMessage("Submitting…");const r=await fetch(`/change-order/${token}/accept`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({decision,signerName:name,signerTitle:title,signerEmail:email,signatureType:"typed",signatureData:decision==="approved"?signature:"Declined",termsAccepted:accepted,customerNote:note})}),b=await r.json();if(r.ok)location.reload();else setMessage(b.error??"Unable to record your decision")}return <section className="estimate-panel"><h2>Customer decision</h2><label>Legal name<input className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label>Title (optional)<input className="input" value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Email<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Typed signature<input className="input" value={signature} onChange={e=>setSignature(e.target.value)}/></label><label>Optional note<textarea className="input" value={note} onChange={e=>setNote(e.target.value)}/></label><label><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)}/> I reviewed this change order and authorize this decision.</label><div className="job-card-actions"><button className="button" disabled={!accepted} onClick={()=>void decide("approved")}>Approve &amp; Sign</button>{allowDecline&&<button className="button button--ghost" disabled={!accepted} onClick={()=>void decide("declined")}>Decline</button>}</div><p role="status">{message}</p></section>}
