import Link from"next/link";export const dynamic="force-dynamic";import AdminTabs from"@/app/admin/_components/AdminTabs";import{listCatalogItems,listCatalogVersions,selectCatalogVersion}from"@/lib/pricing-catalog/repository";import{getMaterialCatalog}from"@/lib/mega-estimator/repository";import WorkbookProductDefaults from"./WorkbookProductDefaults";
export default async function Page({searchParams}:{searchParams:Record<string,string|undefined>}){const productCatalog=await getMaterialCatalog(),versions=await listCatalogVersions(),selected=await selectCatalogVersion({versionId:searchParams.versionId,allowDraft:searchParams.testDraft==="1"}),items=await listCatalogItems({versionId:selected.id,category:searchParams.category,subcategory:searchParams.subcategory,name:searchParams.q,quoteRequired:searchParams.quote==="1"?true:undefined,active:searchParams.inactive==="1"?false:undefined,missingPrice:searchParams.missing==="1"});return <section className="section admin-shell"><div className="admin-hero"><div><p className="admin-kicker">Internal supplier data</p><h1 className="hero-title">Pricing catalogue</h1><p className="hero-subtitle">Server-rendered catalogue costs. Never included in customer proposal data.</p></div><Link className="button" href={`/admin/estimates/new?catalogVersionId=${selected.id}${selected.status==="draft"?"&testDraft=1":""}`}>Test in new estimate</Link></div><AdminTabs currentPath="/admin/pricing-catalog"/><WorkbookProductDefaults initial={productCatalog}/><form className="admin-filter-panel" method="get"><label>Catalogue<select className="input" name="versionId" defaultValue={selected.id}>{versions.map(v=><option value={v.id} key={v.id}>{v.name} — {v.status}</option>)}</select></label><label>Search<input className="input" name="q" defaultValue={searchParams.q}/></label><label>Category<input className="input" name="category" defaultValue={searchParams.category}/></label><label>Subcategory<input className="input" name="subcategory" defaultValue={searchParams.subcategory}/></label><label><input type="checkbox" name="testDraft" value="1" defaultChecked={searchParams.testDraft==="1"}/> Explicit draft test mode</label><label><input type="checkbox" name="quote" value="1" defaultChecked={searchParams.quote==="1"}/> Quote required</label><label><input type="checkbox" name="missing" value="1" defaultChecked={searchParams.missing==="1"}/> Missing price</label><label><input type="checkbox" name="inactive" value="1" defaultChecked={searchParams.inactive==="1"}/> Inactive</label><button className="button">Apply filters</button></form><div className="estimate-panel pricing-catalog-table"><p><b>{selected.name}</b> · {selected.status} · {items.length} results</p><table><thead><tr><th>Category</th><th>Product</th><th>Supplier</th><th>Price</th><th>Unit / coverage</th><th>Source / status</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.category}<small>{i.subcategory}</small></td><td>{i.name}</td><td>{i.supplier??"—"}</td><td>{i.price===null?"Missing":`${i.currency} ${i.price.toFixed(2)}`}</td><td>{i.unit}<small>{i.coverage??"Missing"} {i.coverage_unit??"coverage"}</small></td><td>{i.source_reference??"—"}<small>{i.quote_required?"Quote required":i.active?"Active":"Inactive"}</small></td></tr>)}</tbody></table>{!items.length&&<p>No items match these filters.</p>}</div></section>}
