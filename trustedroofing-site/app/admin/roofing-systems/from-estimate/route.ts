import{NextRequest,NextResponse}from"next/server";import{saveEstimateSystemAsReusable}from"@/lib/roofing-systems/repository";import type{RoofingSystem}from"@/lib/roofing-estimates/pricing";export async function POST(r:NextRequest){try{const id=await saveEstimateSystemAsReusable(await r.json()as RoofingSystem,r.headers.get("x-admin-user")||"admin-token-user");return NextResponse.json({id})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to create reusable system"},{status:400})}}
