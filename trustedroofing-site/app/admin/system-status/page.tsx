import AdminTabs from"@/app/admin/_components/AdminTabs";import{systemReadiness}from"@/lib/readiness/service";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:{result?:string;correlation?:string}}){let report;try{report=await systemReadiness()}catch{report={generatedAt:new Date().toISOString(),correlationId:"readiness-unavailable",checks:[{section:"Application",name:"Readiness service",state:"blocked"as const,message:"Readiness checks could not complete.",feature:"diagnostics",fix:"Confirm Supabase configuration and migration 0028."}],recentErrors:[]}}const sections=[...new Set(report.checks.map(x=>x.section))];return <main className="section admin-shell"><div className="admin-hero"><div><p className="admin-kicker">Deployment diagnostics</p><h1>System Status</h1><p>Safe configuration and schema checks. No customer records are read.</p></div><a className="button" href="/admin/system-status">Retry All</a></div><AdminTabs currentPath="/admin/system-status"/>{searchParams.result&&<div className="estimate-panel" role="status"><b>Active test result</b><p>{searchParams.result}</p>{searchParams.correlation&&<code>{searchParams.correlation}</code>}</div>}<section className="estimate-panel"><h2>Safe Active Tests</h2><div className="job-card-actions">{["connection","schema","bootstrap-history","pdf","proposal-url","change-order-url","email-config"].map(x=><form key={x} action="/admin/system-status/test" method="post"><input type="hidden" name="test" value={x}/><button>{x.replaceAll("-"," ")}</button></form>)}</div><form action="/admin/system-status/test" method="post"><input type="hidden" name="test" value="storage"/><label>Temporary storage test<select name="bucket" className="input"><option>proposal-assets</option><option>project-images</option><option>tender-documents</option><option>project-documents</option></select></label><button>Write / Read / Delete</button></form><details><summary>Optional administrator email test</summary><form action="/admin/system-status/test" method="post"><input type="hidden" name="test" value="email-send"/><label>Administrator email<input className="input" required type="email" name="email"/></label><button>Send Explicit Test Email</button></form></details></section>{sections.map(section=><section className="estimate-panel" key={section}><h2>{section}</h2><div className="status-checks">{report.checks.filter(x=>x.section===section).map((x,i)=><article className="job-card" key={`${x.name}-${i}`}><span className={`job-status job-status--${x.state}`}>{x.state.replace("_"," ")}</span><h3>{x.name}</h3><p>{x.message}</p><small>Affected: {x.feature}</small><p><b>Suggested fix:</b> {x.fix}</p>{x.migration&&<code>Migration {x.migration}</code>}</article>)}</div></section>)}<section className="estimate-panel"><h2>Recent Operational Errors</h2>{report.recentErrors.length?report.recentErrors.map((x:any)=><article key={x.id}><b>{x.feature} · {x.safe_code}</b><p>{x.safe_message}</p><small>{x.occurred_at} · correlation {x.correlation_id}</small></article>):<p>No unresolved administrator-actionable errors were returned.</p>}</section><footer><small>Generated {report.generatedAt} · correlation {report.correlationId}</small></footer></main>}
