import Link from "next/link";
import AdminTabs from "../_components/AdminTabs";
import { listMegaEstimates } from "@/lib/mega-estimator/repository";
export const dynamic="force-dynamic";
const money=(n:number)=>new Intl.NumberFormat("en-CA",{style:"currency",currency:"CAD"}).format(n);
export default async function Page({searchParams}:{searchParams:{q?:string}}){let estimates:Awaited<ReturnType<typeof listMegaEstimates>>=[];let unavailable=false;try{estimates=await listMegaEstimates(searchParams.q)}catch{unavailable=true}return <main className="section admin-shell estimates-home"><div className="admin-hero"><div><p className="admin-kicker">Trusted estimator</p><h1>Roofing Estimates</h1><p>Create, price and send detailed roofing proposals from one worksheet.</p></div><Link className="button estimates-home__new" href="/admin/estimates/new">New Estimate</Link></div><AdminTabs currentPath="/admin/estimates"/><form method="get" className="estimate-panel estimates-search"><label>Search estimates<input className="input" name="q" defaultValue={searchParams.q} placeholder="Customer, property or estimate number"/></label><button className="button">Search</button></form>{unavailable&&<div className="estimate-panel" role="status"><b>Saved estimates are temporarily unavailable.</b><p>You can still start a new estimate. The obsolete Jobs service is not queried by this page.</p></div>}<div className="estimate-panel estimates-table"><table><thead><tr><th>Estimate</th><th>Customer</th><th>Property</th><th>Status</th><th>Last modified</th><th>Final price</th><th></th></tr></thead><tbody>{estimates.map(e=><tr key={e.id}><td>{e.estimateNumber}</td><td>{e.customer}</td><td>{e.property}</td><td>{e.status.replaceAll("_"," ")}</td><td>{new Date(e.updatedAt).toLocaleString("en-CA")}</td><td>{e.finalPrice===null?"—":money(e.finalPrice)}</td><td><Link className="button button--ghost" href={`/admin/estimates/${e.id}`}>Open estimate</Link></td></tr>)}</tbody></table>{!unavailable&&!estimates.length&&<div className="estimates-empty"><h2>No estimates yet</h2><p>Create the first worksheet estimate.</p><Link className="button" href="/admin/estimates/new">New Estimate</Link></div>}</div></main>}
