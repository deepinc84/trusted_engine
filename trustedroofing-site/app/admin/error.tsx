"use client";import{useEffect}from"react";export default function AdminError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error("Admin operation failed",{digest:error.digest})},[error]);return <main className="section admin-shell"><div className="job-error" role="alert"><h1>That admin operation could not be completed</h1><p>Your previously saved data has not been removed. Retry the operation or open System Status to check configuration and migrations.</p>{error.digest&&<p>Reference: <code>{error.digest}</code></p>}<button className="button" onClick={reset}>Retry</button> <a className="button button--ghost" href="/admin/system-status">View System Status</a></div></main>}
