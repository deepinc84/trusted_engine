import{NextRequest,NextResponse}from"next/server";import{getEstimateDraft,saveEstimateDraft}from"@/lib/roofing-estimates/repository";export async function POST(r:NextRequest,{params}:{params:{id:string}}){const original=await getEstimateDraft(params.id);if(!original)return NextResponse.json({error:"Job not found"},{status:404});try{const copy=structuredClone(original);delete copy.id;delete copy.updatedAt;copy.customer={...copy.customer,id:undefined};copy.property={...copy.property,id:undefined};const saved=await saveEstimateDraft(copy,r.headers.get("x-admin-user")||"admin-token-user");return NextResponse.redirect(new URL(`/admin/jobs/${saved.id}`,r.url),303)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to duplicate job"},{status:400})}}
