import{NextRequest,NextResponse}from"next/server";import{createChangeOrder}from"@/lib/projects/repository";export async function POST(r:NextRequest,{params}:{params:{id:string}}){const f=await r.formData();try{const c=await createChangeOrder(String(f.get("projectId")),r.headers.get("x-admin-user")||"admin-token-user");return NextResponse.redirect(new URL(`/admin/jobs/${params.id}/change-orders/${c.revisionId}`,r.url),303)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Change order creation failed"},{status:400})}}
