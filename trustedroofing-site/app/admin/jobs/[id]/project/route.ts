import{NextRequest,NextResponse}from"next/server";import{convertAcceptedJob}from"@/lib/projects/repository";export async function POST(r:NextRequest,{params}:{params:{id:string}}){try{await convertAcceptedJob(params.id,r.headers.get("x-admin-user")||"admin-token-user");return NextResponse.redirect(new URL(`/admin/jobs/${params.id}?tab=project`,r.url),303)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Project conversion failed"},{status:400})}}
