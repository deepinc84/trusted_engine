import{NextRequest,NextResponse}from"next/server";import{reviseChangeOrder}from"@/lib/projects/repository";export async function POST(r:NextRequest,{params}:{params:{id:string;revisionId:string}}){try{const id=await reviseChangeOrder(params.revisionId,r.headers.get("x-admin-user")||"admin-token-user");return NextResponse.redirect(new URL(`/admin/jobs/${params.id}/change-orders/${id}`,r.url),303)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Revision failed"},{status:400})}}
