# Project image and annotation manual test

1. Upload one JPEG roof photo and confirm it appears without exposing its storage path.
2. Upload multiple JPEG/PNG/WEBP photos and verify each creates one reusable source.
3. Paste an image; paste text and verify the UI reports no usable image.
4. Drag an image into the target and confirm it adds rather than replaces a slot.
5. Select and reuse a saved project image.
6. Change crop, zoom, rotation, and Fit/Fill; reload and verify persistence.
7. Add an arrow and circle.
8. Add and edit a text label.
9. Add numbered markers and customer-facing legend descriptions.
10. Save as a new version and verify the original remains.
11. Restore original and confirm its storage object is unchanged.
12. Add a landscape roof-plan page using Fit to Page.
13. Add a two-photo page with captions and comments.
14. Inspect web preview transformations, annotations, captions, and legends.
15. Generate PDF and verify images, annotations, legends, orientation, numbering.
16. Send and verify the locked revision contains image snapshots.
17. Open customer proposal and verify only included images load.
18. Sign and download the signed PDF.
19. Verify it contains the exact sent image pages and annotation versions.
20. Edit the project image and confirm the signed PDF does not change.
