import{createHash}from"node:crypto";export async function temporaryStorageRoundTrip(adapter:{upload:(path:string,data:Buffer)=>Promise<void>;download:(path:string)=>Promise<Buffer>;remove:(path:string)=>Promise<void>},path:string,data=Buffer.from("trusted-readiness-test")){let uploaded=false;try{await adapter.upload(path,data);uploaded=true;const read=await adapter.download(path);return{ok:createHash("sha256").update(read).digest("hex")===createHash("sha256").update(data).digest("hex")}}finally{if(uploaded)await adapter.remove(path)}}
